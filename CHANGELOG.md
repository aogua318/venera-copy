# 更新日志 / Changelog

所有显著改动记录在此文件中。
All notable changes to this project are documented in this file.

## 1.7.2

### 新增
- 结构化导入：压缩包 / 目录导入时自动判断结构——多层无分支的嵌套会拍平为单章漫画（以压缩包 / 文件夹名为漫画名），存在分支（同级多个子目录）则识别为多章节漫画，章节按文件夹名排序导入（目录格式与合并结果一致）
- 多本导入时对每本单独做上述结构判断
- 本地阅读支持嵌套目录的页面递归收集

### 修复
- 合并漫画时，第一本漫画的 `directory` 为绝对路径会导致新漫画名变成"完整路径+漫画名"的问题

### 调整
- 目录导入不再把"章节目录下还有子目录"视为无效漫画

## 1.7.1（最新）

### 新增
- 滚动速度设置支持手动输入数值（100–600000 ms，可超出滑杆范围）

### 修复
- 本地删除的进度框顺序问题：进度框此前被误关闭、确认框残留在屏幕上
- 书架视图模式选择后对话框自动关闭

## 1.7.0

### 修复
- 本地删除后磁盘文件未删除（三轮根因修复：后台 isolate 静默死亡 → 改为主线程逐本删除；SAF 路径需主线程 + `overrideIO` + 原生递归删除；绝对路径拼接错误改用 `baseDir`）
- SAF 目录（`android://...`）删除失败时给出专项提示（重装应用会丢失存储授权，需重新导入目录以重新授权）
- Rhttp 初始化失败（锁定 flutter_rust_bridge 2.11.1）
- 进入阅读器时 `markAsRead` 空指针（上游 bug）

### 新增
- 书架选择模式新增删除操作（本地漫画连同磁盘文件删除，带进度显示）
- 导入 / 合并 / 删除等长时间操作期间自动保持屏幕常亮

### 调整
- 书架列表 / 网格封面改 aspectFit（长边贴边完整展示，不再裁切）

## 1.6.5（内部构建）

### 修复
- 合并大漫画时"有几率闪退"：文件移动/复制移入 isolate，主线程不再长时间阻塞（ANR）
- 本地删除进度框一闪而过：增加 600ms 最短显示时长

### 新增
- 合并进度条：文件粒度百分比实时显示
- 导入：同名漫画自动跳过（覆盖全部导入路径），复制阶段不再改名挪动已存在目录
- 导入进度条：多压缩包 / 多目录 / EhViewer / 本地恢复 / 复制阶段逐项显示"导入中 i/N (xx%)"
- 书架列表视图封面自适应高度填充

## 1.6.4（内部构建）

### 修复
- 合并/删除相关的文件 IO 移出主线程（合并闪退问题的第一轮修复）

## 1.6.3（首个发布版本）

基于上游 [venera-app/venera](https://github.com/venera-app/venera) 的全部增强功能：

### 新增
- 条漫恒速平滑自动滚动：速度可调（每屏毫秒数）、触摸暂停、章末行为可配置（停止 / 自动下一章）、保持屏幕常亮
- 自动播放模式切换：平滑滚动 / 原版定时翻页
- 手柄 / 键盘 / 媒体键支持（Android）：自定义按键映射、开箱即用默认映射、长按不连发
- 独立书架：本地导入（进度条）、排序（含逆序）、列表 / 网格 / 瀑布流三种视图、多选操作（全选 / 反选 / 全不选 / 移出书架）
- 漫画合并：多本本地漫画拖拽排序合并为一本多章节连续漫画，自动清理源漫画
- 阅读页切本：首/末页按钮与所有翻章入口在章末自动切换上一本 / 下一本（跟随书架排序）
- 设为封面：阅读时一键将当前页设为本地漫画封面
- 阅读页末页判定优化：连续模式倒数第二屏即视为章末

### 修复
- 硬件按键不再触发焦点导航（平台通道拦截）
- 删除漫画后阅读进度、收藏、历史同步清理

### 工程适配
- Gradle 8.14 / Kotlin 2.2.20 / NDK 28.2 / mavenCentral 仓库修正
- 包名 `.mod` 后缀（与官方版共存）、无签名配置时回退 debug 签名
- 界面新增简体中文 / 繁体中文翻译
